# Simple DemoQA Automation Testing

This project automates the DemoQA Practice Form using Selenium and Python.
It fills in the form, uploads a file, selects dropdown values, submits the form, and verifies that submission was successful.

# Setup

1. Clone the repository
2. Create and activate a virtual environment
3. Install dependencies
4. Run the test
